services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: wmsucms_web
    env_file:
      - dbvar.env
    volumes:
      - ./your-web-code:/var/www/html
      - ./aiven-certs:/etc/ssl/aiven   # <-- mount your certs folder
    restart: unless-stopped
